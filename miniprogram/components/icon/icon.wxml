<view
  class="custom-class iconfont icon-iconfont{{name}}"
  style="{{ color ? 'color: ' + color : '' }}; {{ size ? 'font-size: ' + size : '16rpx' }}"
  bind:tap="onClick"
>
  <view wx:if="{{ info !== null }}" class="ss-icon__info">{{ info }}</view>
</view>